@inherits LayoutComponentBase
<main class="flex flex-row flex-wrap border-[1px] border-solid border-gray-400 rounded-md min-h-[715px]">
    <section id="left-panel" class="flex flex-col flex-wrap justify-between p-8 rounded-sm w-[18%]">
        <div class="flex flex-col justify-start">
            <HeaderLayout />
            <NavMenu />
        </div>
        <FooterLayout />
    </section>
    <section id="right-panel" class="flex flex-row flex-wrap w-[78%]">
        @* Render by route *@
        @Body
    </section>
</main>
